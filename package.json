{
  "name": "ai-proxy",
  "version": "0.1.0",
  "description": "Local dual-provider proxy: OpenAI (codexer) + Anthropic (teamclaude-rs) with multi-account autobalance.",
  "type": "module",
  "bin": {
    "ai-proxy": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js start --foreground",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "install-binaries": "bash scripts/install-binaries.sh",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "commander": "^14.0.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^6.0.0",
    "vitest": "^3.2.4"
  }
}
